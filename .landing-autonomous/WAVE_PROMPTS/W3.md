# Wave 3 v2.0 — Pipeline + Team + Advisory + Operations (16 images + Animation)

**База:** WAVE_2_ARTIFACT.jsx.

## Задача: s07-s11 + все 16 реальных изображений + Animation Layer

### s09 Team (5 портретов — img01-img05)
Grid 1/2/3/5 cols по breakpoint. Каждая карточка в `<Reveal delay={i*80}>`.

```jsx
{TEAM.map((p, i) => (
  <Reveal key={p.id} delay={i*80}>
    <figure className="card-hover" style={{padding:16, background:'#15181C', borderRadius:12, border:'1px solid #2A2D31'}}>
      <img src={`__IMG_PLACEHOLDER_${p.id}__`} alt={p.alt_ru} width="800" height="1000" loading="lazy" className="w-full aspect-[4/5] object-cover rounded-lg" />
      <figcaption className="mt-3">
        <div style={{fontWeight:600, color:'#EAEAEA'}}>{p.name}</div>
        <div style={{fontSize:13, color:'#8E8E93'}}>{p.slot}</div>
      </figcaption>
    </figure>
  </Reveal>
))}
```

### s10 Advisory (4 sepia портрета — img06-img09)
Staggered с delay i*100, smaller (60% размера team). Hover: rotate(2deg) с transition.

### s07 Pipeline (7 постеров — img10-img16)
Grid карточек с постерами. Filter chips с useState.
- Каждая карточка: `<Reveal>` + `card-hover` + aspect-[2/3] poster + hover zoom on poster (scale 1.05)
- Click на карточку → открывает Modal с детализацией (название/genre/budget/stage/release/synopsis)
- Filter chips: active — background warm-accent, inactive — transparent с border

### s08 Stages (4-col kanban)
Распределение по stages. Каждая колонка в `<Reveal delay={i*100}>`. Карточки внутри — с staggered внутренним delay.

### s11 Operations (6-step SVG process)
Иконки lucide: FileText/CheckCircle/Lightbulb/Video/Megaphone/TrendingUp.
Каждый шаг — `<Reveal delay={i*120}>`. Connector-линии между шагами — SVG path с animated stroke-dashoffset.

## Acceptance W3 v2.0
- 16 `__IMG_PLACEHOLDER_imgNN__` в HTML
- grep "Reveal" ≥10 (5 team + 4 advisory + 7 pipeline + 4 stages + 6 ops = 26)
- grep "card-hover" ≥10
- Modal открывается при клике на project (grep "setModalProject\|showModal")

НАЧИНАЙ.
