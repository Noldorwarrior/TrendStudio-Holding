# Wave 3 v2.1 — Pipeline + Team + Advisory + Operations (Kanban УДАЛЁН)

**База:** WAVE_2_ARTIFACT.jsx. Прочитай главный промт §2, §5.8, §5.10-§5.12.

## ⚠️ КРИТИЧНО v2.1: удалить s08 Stages (Kanban)

См. §2 главного промта. Оставить только s07 Pipeline + s09 Team + s10 Advisory + s11 Operations. **Никакого отдельного StagesSection компонента.** Distribution по стадиям показывается в Roadmap s13 (W4).

## Задача: s07 + s09 + s10 + s11 (БЕЗ s08)

### s07 Pipeline — premium + постеры уже есть
- 7 проектов с постерами (img10-img16 из pack)
- Filter chips: active = glow + scale
- Hover на карточку → 3D tilt (rotateX/rotateY по mousemove)
- Click → modal с полной инфо (synopsis, genre, planned budget, планируемый release, команда)
- **Контекст холдинг→фонд:** в modal добавить «как ваш фонд участвует в этом проекте» + share-of-budget
- Stagger reveal (index × 80ms)

### s09 Team — §5.10 2-state card + gradient border

**5 портретов (img01-img05):**
- **Default:** фото + должность (CEO / Lead Producer / CFO / Head of Distribution / Creative Director)
- **On click:** карточка expand (scale 1.15 + z-index 100 + big shadow), раскрывается: имя, bio bullets, LinkedIn; остальные карточки затухают (scale 0.92 + opacity 0.5)
- Click outside / Esc → возврат
- **Gradient border** вокруг фото `linear-gradient(135deg, #F4A261, #2A9D8F)` 2-3px — убирает color-seam
- Inner vignette на фото
- Spring transition 400ms для expand
- Accessibility: role="button", aria-expanded, Enter/Space, Esc

### s10 Advisory — 4 портрета (img06-img09)
Такой же 2-state как Team, но меньший (60%) + sepia filter. Stagger 100ms.

### s11 Operations 6-step — §5.11 interactive = deeper info

**Click на шаг → раскрывается lane под ним с полной информацией:**
- 1. Scouting: процесс, источники, criteria
- 2. Due Diligence: creative/financial/legal, сроки, deliverables
- 3. Development: script lock, cast attachments, budget lock
- 4. Production: съёмки, cost-review, dashboards для LP
- 5. Marketing & Distribution: OTT/theatrical window, партнёрства
- 6. Exit / IP: library sales, remake rights, sequels

**SVG connector-линии** с animated stroke-dashoffset on scroll. Icons pop-in с spring.

## Acceptance W3 v2.1

- grep «StagesSection\|kanban» → 0 matches (удалено)
- Team/Advisory cards = 2-state (click → expand + others dim)
- grep «gradient border» или `borderImage` или inline gradient-style
- 16 `__IMG_PLACEHOLDER_imgNN__` placeholder'ов
- Pipeline modal содержит «ваш фонд» / «как партнёр»
- Operations 6 steps click → expand

НАЧИНАЙ.
