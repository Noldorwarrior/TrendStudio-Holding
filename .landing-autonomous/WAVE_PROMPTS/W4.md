# Wave 4 — Risk + Roadmap + Scenarios + Regions + Tax + M2 + M3

**Роль:** субагент, волна 4. База — WAVE_3_ARTIFACT.jsx.

---

## Задача: s12-s16 + 2 Marquee симулятора (M2, M3)

### s12 Risks — 3×3 matrix
- Матрица Likelihood (row) × Impact (col) с 12 рисками в клетках
- Клик на риск → Modal (useState) с detail + mitigation
- Цвет: low=#2A9D8F, med=#F4A261, high=#EF4444

### s13 Roadmap — 7-year Gantt SVG
- Горизонталь: годы 2026-2032
- Swimlanes: Fundraising / Portfolio Buildout / Distribution / Exits
- Milestones — пульсирующие точки (CSS @keyframes pulse)

### s14 Scenarios — 4 tabs
- Bear / Base / Bull / Moon
- KPI таблица меняется на активном tab
- Recharts LineChart: 4 линии (активная — bold)

### s15 Regions — SVG map РФ (упрощённо)
- 8 ФО как path'ы (можно треугольники/прямоугольники вместо геометрии)
- Hover → tooltip с # проектов в регионе

### s16 Tax Credits
- 4 карты: Фонд кино / Минкультуры / Региональные rebate / Digital bonus
- Поля: условия, % возврата, пример расчёта

### M2 Pipeline Builder (MARQUEE — drag-drop)
```jsx
// Native HTML5 DnD, без либ
<div onDragStart={handleStart} onDragOver={e => e.preventDefault()} onDrop={handleDrop}>...</div>

// 7 проектов draggable из left-rail
// 4 stage columns как drop-zones
// Real-time: weightedIRR = Σ(project.irr_estimate * project.budget_weight) / total_weight
// Warning chip если в одной стадии > 3 проектов
// Button "Reset to Canon" → restoreInitial state
```

### M3 LP Sizer (MARQUEE)
```jsx
// Input: target_irr (5-30%), investment (10-500 млн), horizon (5-10 лет)
// Output:
//   - Probability reaching target: count(M1_dist >= target_irr) / 10000
//   - Recommended stake % (linear interpolation)
//   - AreaChart: expected cashflow year-by-year
//   - Warning если target_irr > 25% ("превышает P90 симуляций")
```

---

## Acceptance
- 12 рисков в матрице
- 4 сценария работают (tab switch)
- M2: drag проект → меняется IRR (real-time)
- M3: вводишь target → появляется probability
- Нет запрещённых API

НАЧИНАЙ.
