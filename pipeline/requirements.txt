# Холдинг Кино Finmodel Pipeline — pinned dependencies
# FIX-11: Upper version bounds added for reproducibility (DD requirement)

# Pydantic v2 для контрактов
pydantic>=2.5,<3.0

# YAML parser
PyYAML>=6.0.1,<7.0

# Excel / Word / PDF
openpyxl>=3.1.2,<4.0
python-docx>=1.1.0,<2.0

# Numerical / statistical
numpy>=1.26,<3.0
scipy>=1.11,<2.0
pandas>=2.1,<3.0

# Testing
pytest>=7.4,<9.0
pytest-html>=4.1,<5.0
pytest-cov>=4.1,<6.0
hypothesis>=6.92,<7.0

# Mutation testing
mutmut>=2.4,<3.0

# Code quality (optional)
black>=23.12,<25.0
ruff>=0.1.9,<1.0
mypy>=1.8,<2.0

# Visualization (для MC histogram)
matplotlib>=3.8,<4.0

# Semantic search (для navigation/ontology.fts5)
# sqlite3 — встроено в Python
