# Wave 2 v2.0 — Economics + M1 Monte-Carlo (с Animation Layer)

**База:** WAVE_1_ARTIFACT.jsx (уже содержит useReveal/Reveal/Tooltip/CountUp).

## Задача: s04-s06 + M1 Monte-Carlo + Animation Layer ко всему

Логика секций — идентична v1.2 W2.md:
- s04 Fund Structure (Recharts PieChart 85/15)
- s05 Economics (4 KPI + waterfall SVG)
- s06 Returns (2 tabs + LineChart 7 лет)
- M1 Monte-Carlo: 10 000 runs, 3 слайдера, P10/P25/P50/P75/P90

## Animation Layer — ОБЯЗАТЕЛЬНО добавить:

1. **s04 Fund Structure**: каждая из 3 фактов-карт — `<Reveal delay={i*100}>`. Пончик (PieChart) — появляется с анимацией Recharts (по умолчанию).

2. **s05 Economics**: 4 KPI-карты с CountUp (2% / 20% / 8% / 100%). Staggered reveal.

3. **s06 Returns**: таблица IRR/MOIC/TVPI/DPI с CountUp на числах. LineChart с animationBegin/animationDuration.

4. **M1 Monte-Carlo**:
   - Слайдеры: при `onChange` debounce 150ms → `runMonteCarlo` → гистограмма с animation
   - Результаты P10/P25/P50/P75/P90 — с CountUp при изменении параметров
   - Кнопка "Run 10 000 simulations" с loading state (disable + spinner) 500ms
   - **Tooltip** над терминами "P50", "P90", "hit rate", "avg multiple", "loss rate"

5. **PE-термины с Tooltip**:
   - `hit rate` → "доля проектов, которые попали в хит-категорию"
   - `avg multiple` → "среднее, во сколько раз вырос капитал на хит-проектах"
   - `loss rate` → "доля проектов с полной потерей капитала"
   - `MOIC`, `TVPI`, `DPI` → кратко: "Multiple of Invested Capital — во сколько раз ваш капитал умножился"

## Acceptance W2 v2.0

- grep "Reveal" ≥8 матчей (применено к s04-s06 + M1 UI)
- grep "CountUp" ≥6 матчей (KPIs)
- grep "Tooltip" ≥5 матчей (на PE-термины)
- M1 P50 в диапазоне [13.5, 14.5] при дефолтах

НАЧИНАЙ.
