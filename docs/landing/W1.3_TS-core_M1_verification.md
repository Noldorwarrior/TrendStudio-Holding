# W1.3 TS-core — Верификация М1 «Базовая»

**Дата:** 2026-04-20
**Режим:** Cowork (исполнитель — Claude Sonnet 4.6)
**Пресет:** М1 «Базовая» = №1 точный перенос контрактов + №2 проверка выполнения всех пунктов запроса

---

## 1. Скоуп

Модули ядра Landing v1.0:

| Файл | Байт | SHA-256 (первые 16) |
|---|---:|---|
| `src/landing/core/eventbus.js` | 4 575 | `b879f2bc54d3b541…` |
| `src/landing/core/i18n.js` | 7 669 | `aa8d6f55bea91003…` |
| `src/landing/core/a11y.js` | 7 327 | `5580bf6df84182c9…` |
| `src/landing/core/orchestrator.js` | 8 254 | `ac765bd20ac5f8c4…` |
| `src/landing/core/index.js` | 1 051 | `22b999d27cf43b6c…` |
| `src/landing/core/__tests__/eventbus.test.js` | 4 697 | `25db983103e306ae…` |
| `src/landing/core/__tests__/i18n.test.js` | 5 900 | `44061159d2c4017e…` |
| `src/landing/core/__tests__/a11y.test.js` | 7 551 | `20c25c3301673764…` |
| `src/landing/core/__tests__/orchestrator.test.js` | 9 468 | `6c20f619b52c2e74…` |

**Dual-location parity:** все 9 файлов идентичны в обеих локациях (9/9 MATCH).

---

## 2. Механизм №1 — точный перенос контрактов

Источник контрактов: `HANDOFF_Landing_v1.0_Stage_A_to_B.md` (B1a), `B1b.1_DETAIL`, `B1b.2_i18n_blueprint`, `B1b.3_wave_plan.json` (после hotfix B1b.4), `CLAUDE.md` (TrendStudio-Holding).

### 2.1 TS.EventBus

| Контракт | В коде | Статус |
|---|---|---|
| `on(event, handler)` → unsubscribe fn | `EventBus.on` возвращает функцию | PASS |
| `off(event, handler)` → bool | `EventBus.off` возвращает `true`/`false` | PASS |
| `once(event, handler)` → unsubscribe fn | реализован с self-unsubscribe | PASS |
| `emit(event, payload)` → count | возвращает число вызванных handlers | PASS |
| Wildcard `'*'` | получает все события с `(payload, name)` | PASS |
| Snapshot-iteration при emit | `Array.from(handlers)` до обхода | PASS |
| Exception-safety | `try/catch` на каждый handler, `console.error` | PASS |
| `reset()` | полная очистка | PASS |

### 2.2 TS.I18N

| Контракт | В коде | Статус |
|---|---|---|
| `init({ ru, en, locale? })` | TypeError на некорректный вход, RangeError на неизвестный locale | PASS |
| `format(key, vars?)` | flat-match → nested-walk, `{var}` + `{count, plural, ...}` | PASS |
| `setLocale(locale)` → emit('locale:changed') | emit только при реальной смене, возвращает bool | PASS |
| `getLocale() / getAvailableLocales()` | копия `['ru','en']` | PASS |
| `has(key)` | резолв в активной локали | PASS |
| ICU-lite plural | `Intl.PluralRules(locale).select(count)`, `#` подстановка, явные `=0/=1` | PASS |
| Unknown key → warn + возврат самого key | `console.warn` + `return key` | PASS |
| `reset()` | возврат к default locale + пустые словари | PASS |

### 2.3 TS.A11y

| Контракт | В коде | Статус |
|---|---|---|
| `mount({ document })` | создаёт 2 region (polite `role=status` + assertive `role=alert`), `aria-atomic=true`, SR-only inline style | PASS |
| Идемпотентный `mount` | повторный вызов — no-op | PASS |
| `unmount()` | удаляет regions, сбрасывает state | PASS |
| `announce(text, { assertive? })` | emit('a11y:announce'), `queueMicrotask` для re-announce | PASS |
| `trapFocus(element)` | фокус на первом focusable, Tab/Shift+Tab wrap | PASS |
| `releaseFocus()` | возврат фокуса на `prevActive` | PASS |
| `isMounted()` / `reset()` | синхронно с внутренним state | PASS |
| DI `document` | без mount — `announce` no-op с warn (SSR-safe) | PASS |

### 2.4 TS.Orchestrator

| Контракт | В коде | Статус |
|---|---|---|
| `register(name, initFn)` | дубль — warn, не падает | PASS |
| `boot(opts)` → `Promise<{modules, state}>` | последовательный await, emit('boot:ready') | PASS |
| Повторный boot — no-op с warn | `_booted` flag | PASS |
| URL-state каскад query > hash > sessionStorage | `_hydrate()` в правильном порядке | PASS |
| **NO localStorage** (INV-06) | regex-check CLEAN; только sessionStorage | PASS |
| `getState()` → копия | `{ ..._state }` | PASS |
| `setState(patch)` → emit('state:changed') только при реальном diff | dedup через `_sanitize + diff` | PASS |
| `persist:false` не пишет в storage | опциональный флаг | PASS |
| `reset()` | полный teardown | PASS |

### 2.5 Barrel `index.js`

| Контракт | В коде | Статус |
|---|---|---|
| Named exports: `EventBus`, `I18N`, `A11y`, `Orchestrator` | все 4 | PASS |
| Namespace `TS` = `Object.freeze({...})` | без side-effects на global | PASS |
| Default export `TS` | есть | PASS |

---

## 3. Механизм №2 — проверка выполнения всех пунктов запроса

Исходный перечень из `wave_plan.json` (W1.3):

| Требование | Выполнено | Примечание |
|---|---|---|
| 4 модуля ядра (EventBus + I18N + A11y + Orchestrator) | ДА | 4 × .js |
| Barrel-export `index.js` с namespace `TS` | ДА | `Object.freeze` + default |
| Unit-тесты для каждого модуля | ДА | 4 × .test.js, 63 теста total |
| node:test + node:assert (zero deps) | ДА | `import { test, beforeEach } from 'node:test'` |
| Dual-location (/Холдинг + /TrendStudio-Holding) | ДА | 9/9 sha256 MATCH |
| Тесты PASS в обеих локациях | ДА | 63/63 PASS, RC=0, RC=0 |
| INV-06: нет localStorage / eval / new Function | ДА | regex-check CLEAN |
| i18n: 9 namespaces, 424 keys (из W1.1) | наследуется | на W1.3 проверка не нужна |
| EventBus exception-safety | ДА | handler throw не ломает других |
| Events: `locale:changed`, `boot:ready`, `a11y:announce`, `state:changed` | ДА | все 4 emitted |

---

## 4. Итоговый результат

- **Механизм №1:** 34/34 контрактных пункта PASS
- **Механизм №2:** 10/10 пунктов запроса PASS
- **Запреты INV-06:** CLEAN
- **Dual-location parity:** 9/9 MATCH
- **Тесты:** 63/63 PASS × 2 локации = 126/126 PASS

**Вердикт:** GO W1.3 CLOSED. Готов к W1.4 (CSS tokens).

---

## 5. Границы верификации

М1 «Базовая» проверяет:
- API-сигнатуры (что контракты точно реализованы)
- Выполнение всех пунктов запроса

НЕ проверяет (это предмет финального W1-closure П5 «Максимум»):
- Перформанс под нагрузкой
- Скрытые допущения, парадоксы, граф причин-следствий
- Моделирование аудитории
- Cross-wave согласованность с будущими W2-W6

Для W1-closure (task #20) запланирован П5 32/32.
