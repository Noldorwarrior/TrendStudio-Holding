# Wave 2 — Economics + M1 Monte-Carlo

**Роль:** субагент, волна 2. База — `WAVE_1_ARTIFACT.jsx`.

**Вход:** canon/* + WAVE_1_ARTIFACT.jsx (прочитать для контекста компонентов)
**Выход:** WAVE_2_ARTIFACT.jsx (секции s04-s06 + M1) + WAVE_2_OUTPUT.md

---

## Задача: s04-s06 + Marquee M1

### s04 Fund Structure
- Recharts PieChart: LP 85% + GP 15% (цвета #F4A261 / #2A9D8F)
- 3 факт-карты: Commitment, Vintage 2026, Jurisdiction (из canon.fund)

### s05 Economics
- 4 KPI (Management Fee 2% / Carry 20% / Hurdle 8% / Catch-up 100%) — из canon.economics
- Waterfall SVG: 4-tier breakdown + tooltips

### s06 Returns
- 2 tabs (Internal / Public) — useState
- Таблица IRR/MOIC/TVPI/DPI (якоря: Internal 24.75%, Public 20.09%)
- Recharts LineChart: IRR trajectory по годам 1-7

### M1 Monte-Carlo (MARQUEE)

**Контракт:**
- 10 000 runs per click кнопки "Run 10 000 simulations"
- 3 слайдера: hit_rate (10-40%), avg_multiple (1.5x-4.0x), loss_rate (5-25%)
- Output: P10/P25/P50/P75/P90 IRR (текст) + BarChart гистограмма 20 bins
- **Якорь:** при дефолтах (hit=25%, avg=2.3x, loss=12%) → P50 ∈ [13.5, 14.5]
- Debounce 150ms

**Код:**
```jsx
function mulberry32(seed) {
  return function() {
    var t = seed += 0x6D2B79F5;
    t = Math.imul(t ^ t >>> 15, t | 1);
    t ^= t + Math.imul(t ^ t >>> 7, t | 61);
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}

function runMonteCarlo(runs=10000, hitRate=0.25, avgMult=2.3, lossRate=0.12, seed=42) {
  const rand = mulberry32(seed);
  const results = [];
  for (let i = 0; i < runs; i++) {
    let portfolio = 0;
    for (let p = 0; p < 7; p++) {
      const r = rand();
      if (r < lossRate) portfolio += -1;
      else if (r < lossRate + (1 - hitRate - lossRate)) portfolio += 0.5;
      else portfolio += avgMult;
    }
    const multiple = portfolio / 7 + 1;
    const irr = multiple > 0 ? (Math.pow(multiple, 1/7) - 1) * 100 : -100;
    results.push(irr);
  }
  results.sort((a,b) => a - b);
  return {
    p10: results[Math.floor(runs*0.1)],
    p25: results[Math.floor(runs*0.25)],
    p50: results[Math.floor(runs*0.5)],
    p75: results[Math.floor(runs*0.75)],
    p90: results[Math.floor(runs*0.9)],
    distribution: results
  };
}
```

## Acceptance
- WAVE_1_ARTIFACT.jsx код скопирован в WAVE_2_ARTIFACT.jsx + добавлены s04-s06 + M1
- grep якорей "24.75" "20.09" "13.95" — всё найдено
- useState для tabs/slider'ов — присутствует
- Нет запрещённых API

НАЧИНАЙ.
