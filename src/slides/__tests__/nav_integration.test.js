/* Phase 2C slides nav integration test (Wave 1 placeholder) */

describe('Phase 2C slides nav integration (skeleton)', () => {
  it.skip('slide lifecycle: enter → exit → re-enter is idempotent', () => {
    // TODO(Wave 6): after each slide is implemented
  });

  it.skip('navigation preserves TS.URL hash/query state', () => {
    // TODO(Wave 6)
  });

  it.skip('no memory leak over 10x nav 1→25→1', () => {
    // TODO(Wave 6) — per QA_PERF checklist
  });
});
