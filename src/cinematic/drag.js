// G15 — TS.Drag framework with keyboard fallback
// See: Handoff_Phase2C/10_modules/g15_drag/MODULE_PROMPT.md
// Wave 1 SKELETON — implementation comes in Wave 4.

(function (global) {
  'use strict';
  global.TS = global.TS || {};
  global.TS.Drag = {
    enable: function () { throw new Error('TS.Drag.enable not implemented (Wave 4)'); },
    _skeleton: true
  };
})(typeof window !== 'undefined' ? window : globalThis);
