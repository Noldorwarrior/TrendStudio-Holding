# 00_START_NOW.md — Expedited запуск 16 апреля 2026

**Дедлайн:** сегодня 23:00 МСК. Full scope (S40–S51).
**Старт:** 10:30 МСК.
**Бюджет времени:** 12,5 часов.

---

## 0. Прочитай перед стартом

Файлы читать в порядке: `00_START_NOW.md` (сначала) → `00_CC_PROMPT.md` → `10_CLAUDE.md` → `20_SUBAGENT_GRAPH.md` → `30_DOCUMENT_MAP.md` → `40_CONTRACTS.md` → `50_VERIFICATION.md`.

При конфликте инструкций: **этот файл имеет приоритет** над `00_CC_PROMPT.md` по вопросам тайминга и параллелизации.

---

## 1. Таймлайн с чекпойнтами

| Время МСК | Фаза | Субагенты | Чекпойнт оператору |
|-----------|------|-----------|--------------------|
| 10:30–11:00 | Setup | — | Ветка создана, CLAUDE.md в корне, `build_html.py` BUDGET=650_000 пушнут |
| 11:00–12:30 | S40+S41 | инфра + TS.Charts core | Core зарегистрирован, 8+ unit-тестов passing. **Чекпойнт 12:30** |
| 12:30–16:30 | S42–S48 ПАРАЛЛЕЛЬНО | 7 чартов | Каждые 30 мин — краткий статус по 7 потокам. **Чекпойнт 16:30** |
| 16:30–18:30 | S49+S50 ПАРАЛЛЕЛЬНО | controls + drilldown | URL-state работает, Modal открывается. **Чекпойнт 18:30** |
| 18:30–21:00 | S51 | QA + build | ≥ 70 тестов green, HTML ≤ 650 KB. **Чекпойнт 21:00** |
| 21:00–22:00 | S51 | P5 32/32 docx | Отчёт готов, вердикт ≥ 30/2/0 |
| 22:00–23:00 | push + tag | — | Tag `v1.2.0-phase2b` в origin, commit hash пользователю |

**Жёсткое правило:** если чекпойнт пропущен на >30 минут — автоматически сработать план `ROLLBACK_AT_<time>` (см. §4).

---

## 2. Максимальная параллелизация S42–S48

CC обязан запустить **7 субагентов одновременно** через 7 независимых Task-вызовов в одном сообщении. Не последовательно, не батчами по 2-3, а разом все 7.

Каждый получает прицельный промт с тремя блоками:

```
1. КОНТЕКСТ (одинаковый для всех):
   - Прочитай 10_CLAUDE.md, 40_CONTRACTS.md
   - Твой owner-файл: src/charts/<your_chart>.js + test
   - Не трогай чужие файлы

2. ЗАДАЧА (индивидуальная):
   - Реализовать <имя чарта> по контракту TS.Charts.register
   - Data-dep: <путь в deck_data_v1.2.0.json>
   - i18n-ключи: <namespace>
   - ≥ 5 тестов

3. DOD (Definition of Done):
   - Tests passing
   - Commit: S<NN>: <чарт>
   - Сигнал о готовности
```

Критически: **каждый субагент работает в изолированном sandbox-worktree** (`isolation: "worktree"`), чтобы 7 параллельных коммитов не конфликтовали. После всех 7 — merge в основную ветку одним rebase-pass.

---

## 3. Синхронные артефакты (нельзя параллелить)

Эти правки **сериализуются** через S41 до старта параллельной фазы:

- `data_extract/deck_data_v1.2.0.json` — S41 валидирует схему и добавляет недостающие секции (`sensitivity`, `mc`, `peers`, `cashflow.yearly`)
- `i18n/ru.json` + `i18n/en.json` — S41 резервирует namespace-префиксы под каждый чарт комментариями в начале файла (документ-маркеры), чтобы 7 параллельных APPEND-коммитов не столкнулись

---

## 4. Планы отката по времени

### ROLLBACK_AT_16:30 (S42–S48 не закрылись)
- Cut S48 (Peers) — передать в Phase 2C
- Cut самого медленного чарта из оставшихся
- Продолжить с 5 чартами

### ROLLBACK_AT_18:30 (S49/S50 не закрылись)
- Cut S50 (Drill-down) — оставить tooltip-only
- S49 sliders оставить в минимальном виде: только scenario switcher, без 3 sliders
- Продолжить

### ROLLBACK_AT_21:00 (QA не прошла)
- Если < 28/32 зелёных или > 2 жёлтых → **не релизим**
- Коммит `S51: partial v1.2.0-phase2b-rc1` + отчёт с перечнем оставшихся проблем
- Hotfix утром 17.04

### ROLLBACK_BUDGET (HTML > 650 KB)
- Включить lazy-load для Pipeline Gantt + MC Distribution
- Если > 650 KB повторно → drop Peers assets → drop Drill-down контента (оставить ссылку «см. в LP-deck v1.2.1»)

---

## 5. Коммуникация с оператором

Каждые **30 минут** CC обязан выдавать короткий статус:

```
[ЧЧ:ММ] Фаза: <...>
├─ S42: ✅ merged / 🔄 in-progress (ETA 14:00) / ❌ blocked
├─ S43: ...
├─ S44: ...
└─ ...
Бюджет HTML: ___ / 650 000
Тесты: ___ passing
Риски: <если есть>
```

Оператор отвечает только при красном флаге или на чекпойнте.

---

## 6. Scope-cut decision matrix

Если в любой момент wall-clock показывает отставание >45 минут:

| Отставание | Действие |
|-----------|----------|
| 45 мин | Предупреждение оператору + ускорение (меньше итераций тестов) |
| 90 мин | Cut S48 Peers автоматически |
| 150 мин | Cut S50 Drill-down автоматически |
| 210 мин | Cut MC stress slider (оставить только rate/horizon) |
| 270 мин | Stop + hotfix на утро 17.04 |

---

## 7. Особые настройки для ускорения

1. **Не прерывать на мелкие уточнения.** CC работает автономно. Оператор подтверждает только на чекпойнтах.
2. **Тесты пишутся ОДНОВРЕМЕННО с кодом**, не в конце. Если субагент закончил только код без тестов — DoD не выполнен.
3. **Synthetic fallback** для Peers разрешён (флаг `"synthetic": true` в JSON + commit-msg маркер).
4. **EN-ключи** — допустимо временно использовать stub `[EN:<ru>]` для новых ключей; S51 подсчитает, в отчёте отметит, не блокирует релиз.
5. **Перерывы запрещены** внутри одного субагента (не ставить pause/continue-инструкции).

---

## 8. Команды оператора (быстрые)

```bash
# Создать ветку и подготовить окружение
cd /path/to/TrendStudio-Holding
git checkout -b claude/deck-v1.2.0-phase2b af54bc1
cp "/sessions/bold-magical-gauss/mnt/Холдинг/Phase2B_CC_Package/10_CLAUDE.md" CLAUDE.md
git add CLAUDE.md && git commit -m "chore: add CLAUDE.md for phase 2B"

# Поднять бюджет (сделает S40, но для безопасности можно ручно проверить)
grep "BUDGET" scripts/build_html.py

# Финальный push (по готовности в 22:30)
cd /tmp && mkdir -p tsh && cd tsh
git --git-dir=/path/to/TrendStudio-Holding/.git bundle create ts.bundle claude/deck-v1.2.0-phase2b
# ... push через SSH-ключ
GIT_SSH_COMMAND="ssh -i ~/.git-ssh/id_ed25519 -o IdentitiesOnly=yes" \
  git --git-dir=/path/to/TrendStudio-Holding/.git push -u origin claude/deck-v1.2.0-phase2b
GIT_SSH_COMMAND="ssh -i ~/.git-ssh/id_ed25519 -o IdentitiesOnly=yes" \
  git --git-dir=/path/to/TrendStudio-Holding/.git push origin v1.2.0-phase2b
```

---

## 9. Готов к старту?

Если да — скопировать `00_CC_PROMPT.md` как первое сообщение в CC и добавить приписку:

> **РЕЖИМ: EXPEDITED.** Сначала прочитай `00_START_NOW.md` — там таймлайн, правила параллелизации и откаты. Full scope, дедлайн сегодня 23:00 МСК. Каждые 30 минут — краткий статус оператору.
