# Wave 4 v2.2 — Risks + Roadmap + Scenarios + Regions + Tax + M2 + M3 (grep enforced)

**База:** WAVE_3_ARTIFACT.jsx. Главный промт §4.11-§4.18, §5 (fix-спеки).

## GREP-CONTRACTS

### s12 Risks 3×3 — MUST_CONTAIN
- modal pattern: `role="dialog"` или `<Modal` или `showRiskModal`
- 12 risks (array length)

### s13 Roadmap — MUST_CONTAIN (CRITICAL)
- **swimlanes:** строка `swimlane` или `swimLane` или `lane-` минимум 4 раза
- **scrubber/playhead:** `scrubber` или `playhead` или `yearSelector`
- `animationIterationCount: 3` (не infinite)

### s13 Roadmap — MUST_NOT_CONTAIN
- `animation:.*pulse.*infinite` — беcконечная анимация сломана
- `animationIterationCount: *infinite`

### s16 Tax Credits — MUST_CONTAIN (CRITICAL)
- `Math.min.*budget.*0\.85` или `totalSubsidy.*0\.85` или комментарий `cap.*85`
- effective rate НЕ должна превышать 85% в UI

### s16 Tax Credits — MUST_NOT_CONTAIN
- `Эффективная ставка.*10[0-9]%` (100-109%)
- `102%` как значение

### M2 Pipeline Builder — MUST_CONTAIN (КРИТИЧНО, регресс в v2.1)
- **KPI-row** сверху: grep одновременно
  - `Portfolio size` или `Бюджет портфеля`
  - `Weighted IRR` или `weightedIRR`
  - `Проектов в портфеле` или `Проектов` + `/ 7`
- `"Вернуть к исходному"` (кнопка reset)
- **Rail drop-target:** `rail.*onDrop` или `onDrop.*rail` (drag из колонки в rail возвращает)
- **Постеры в карточках:** `__IMG_PLACEHOLDER_img1` (10-16) в render карточек M2
- **FLIP-anim:** `element.animate\(` или `transition.*cubic-bezier` на project cards

### M2 — MUST_NOT_CONTAIN
- `"Reset to Canon"` (старое имя кнопки)

### M3 Commitment Calc — MUST_CONTAIN
- **Tier badges:** `Partner`, `Lead Investor`, `Anchor Partner` — все 3
- `commitment.*вашего фонда` или `ваш фонд получит`
- `MOIC` = 3.62 (проверка математики)
- `your_take` = tier1 + tier3 + tier4

### M3 — MUST_NOT_CONTAIN
- `"Supporter"` (старый tier LP)
- `"Sponsor"` (старый tier LP)

## Типовой код

### M2 KPI-row (НЕ забыть! регрессировало в v2.1)

```jsx
<div style={{
  display:'grid', gridTemplateColumns:'1fr 1fr 1fr', gap:24,
  padding:24, background:'rgba(21,24,28,0.6)', backdropFilter:'blur(12px)',
  border:'1px solid #2A2D31', borderRadius:14, marginBottom:24
}}>
  <div>
    <div style={{fontSize:12, color:'#8E8E93', textTransform:'uppercase'}}>Portfolio size (бюджет)</div>
    <div style={{fontSize:36, fontFamily:"'Playfair Display'", color:'#F4A261'}}>
      <CountUp end={totalBudget}/> <span style={{fontSize:16, color:'#8E8E93'}}>млн ₽</span>
    </div>
  </div>
  <div>
    <div style={{fontSize:12, color:'#8E8E93', textTransform:'uppercase'}}>Weighted IRR</div>
    <div style={{fontSize:36, fontFamily:"'Playfair Display'", color:'#2A9D8F'}}>
      <CountUp end={weightedIRR} decimals={2}/><span>%</span>
    </div>
  </div>
  <div>
    <div style={{fontSize:12, color:'#8E8E93', textTransform:'uppercase'}}>Проектов в портфеле</div>
    <div style={{fontSize:36, fontFamily:"'Playfair Display'", color:'#EAEAEA'}}>
      <CountUp end={stagedProjects.length}/> <span style={{fontSize:16, color:'#8E8E93'}}>/ 7</span>
    </div>
  </div>
</div>
```

### Tax Credits cap 85%

```jsx
function TaxSummary({ budget }) {
  const rawTotal = PROGRAMS.reduce((a, p) => a + p.calcSubsidy(budget), 0);
  const cappedTotal = Math.min(rawTotal, budget * 0.85);  // cap на 85%
  const effectivePct = budget > 0 ? (cappedTotal / budget) * 100 : 0;
  // effectivePct теперь максимум 85
  ...
}
```

### Roadmap swimlane + no-infinite pulse

```jsx
const SWIMLANES = ['Fundraising', 'Development', 'Pre-production', 'Production', 'Post-production', 'Distribution', 'Exits'];

<div className="roadmap-container">
  {SWIMLANES.map((lane, i) => (
    <div key={lane} className="swimlane" data-lane={i}>
      <div className="swimlane-label">{lane}</div>
      {PROJECTS_TIMELINE.filter(p => p.lanes.includes(lane)).map(p => (
        <div key={p.id} className="milestone-dot" 
          style={{
            animationIterationCount: 3,  // НЕ infinite!
            animation: 'pulse 2s ease-in-out 3'  // 3 раза и стоп
          }}/>
      ))}
    </div>
  ))}
  <input type="range" className="scrubber-playhead" min={2026} max={2032}/>
</div>
```

## Acceptance W4 v2.2

acceptance.sh --wave=4 — очень строгий gate. M2 KPI-row обязательна, иначе fail.

НАЧИНАЙ.
